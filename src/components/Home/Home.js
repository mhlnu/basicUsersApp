import React from "react";
import { Container, Header } from "semantic-ui-react";

const Home = props => {
    return (
        <Container>
            <Header as="h2">Hello</Header>
            <p>Choose your poison from the menu above</p>
        </Container>
    );
};

export default Home;
